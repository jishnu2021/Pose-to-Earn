// Optional: Interact with web pages if needed

console.log("Cross-chain Crypto Wallet content script loaded");

// Example function to interact with the web page
const interactWithPage = () => {
  // Logic to interact with the current web page
};

// You can add event listeners or logic to interact with the web page
document.addEventListener('DOMContentLoaded', interactWithPage);
